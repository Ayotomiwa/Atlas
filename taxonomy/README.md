# Atlas taxonomy

`types.yaml`, `relationships.yaml`, and `statuses.yaml` are the machine-readable contracts used by lint and map generation.

Changes must update affected templates/scripts, pass self-test and lint, and go through human review.
