# Linter fixtures

`valid/` contains minimal examples; `invalid/` reserves one file per ERROR rule for self-test evolution. Fixtures use package `fixtures` and are excluded from production indexes, maps and ID uniqueness.
