should_trigger:
- capture reusable fact
should_not_trigger:
- unrelated generic question
expected_reads:
- package.md
expected_writes:
- task-appropriate files
forbidden_actions:
- curated write
outcome_assertions:
- follows V1 contract
must_contain:
- staging bucket
- Never write curated
