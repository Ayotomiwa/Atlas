should_trigger:
- setup Atlas in repo
should_not_trigger:
- unrelated generic question
expected_reads:
- package.md
expected_writes:
- task-appropriate files
forbidden_actions:
- delete existing CLAUDE
outcome_assertions:
- follows V1 contract
must_contain:
- Preserve every existing user line
- managed block
