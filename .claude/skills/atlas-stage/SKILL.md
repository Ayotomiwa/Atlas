---
name: atlas-stage
description: Use only after an explicit request to capture one coherent reusable TeamA engineering fact as attributable staging evidence without curating it.
allowed-tools: Read, Grep, Glob, Bash, Write, Edit
---

# atlas-stage

Read `../_shared/runtime.md`, `../_shared/answer-provenance.md`, `_staging/README.md`, then the selected bucket README/template. Do not use a subagent.

1. Confirm the user explicitly wants persistence. Obtain approval before persisting knowledge from a private conversation or private external source.
2. Search staging and curated pages, indexes and exact routes for semantic duplicates. Choose the bucket by the primary reusable fact; split only independent semantic boundaries and route broad investigation to `atlas-onboard-repository`.
3. Allocate a deterministic `STG-YYYYMMDD-<slug>` ID. Capture one coherent record using the common envelope and bucket-specific body, preserving source references, known/possible states, evidence gaps and suggested curated targets.
4. Flag sensitive artifacts without copying values. Never revise committed evidence; corrections are new staging records and only lifecycle may later alter top-level `status`.
5. Never write curated knowledge or generated artifacts. Run permitted validation unless explicitly deferred.
6. Report the new record, source evidence, duplicate-search routes, excluded claims and validation state with references.
