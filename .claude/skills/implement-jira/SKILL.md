---
name: implement-jira
description: Implement a Jira item using resolved TeamA standards rather than hard-coding team rules in the skill.
allowed-tools: Read, Grep, Glob, Bash, Write, Edit
---

# implement-jira

Resolve the home package through local CLAUDE.md/Atlas package.md. Resolve the applicable Jira implementation standard starting at `_curated/standards/jira/index.md` and follow `atlas.extends` specialisation. Resolve relevant component and flow context. Read the product repository's own build/test instructions. Implement in small steps, validate locally and report Atlas standards/pages used. If reusable new context is discovered, ask user approval before staging it. Never mutate curated Atlas knowledge as a side effect.
