---
name: atlas-stage
description: Use only after an explicit request to capture one coherent reusable datalens engineering fact as attributable staging evidence without curating it.
---

# atlas-stage

Read shared runtime/provenance, staging root policy and selected bucket README/template. Do not delegate.

1. Confirm explicit persistence; obtain approval for private conversation/external knowledge.
2. Search staging plus typed curated `find`, exact routes and relevant indexes for semantic duplicates. Never silently select ambiguity. Choose the primary-fact bucket and split only independent boundaries. Route broad investigation to repository onboarding and incremental merged-default-branch ranges to `atlas-stage-changes`.
3. Allocate a deterministic staging ID and capture one coherent record with precise source references, known/possible states, gaps and suggested targets. For question-derived evidence, cite every originating qualified `<record-id>#<question-id>` in the body.
4. Flag sensitive artifacts without values. Never revise committed evidence or write curated/generated files.
5. Report the record, sources, duplicate routes, excluded claims and permitted validation state with references.
