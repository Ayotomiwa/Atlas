---
name: atlas-stage
description: Use only after an explicit request to capture one coherent reusable datalens engineering fact as attributable staging evidence without curating it.
---

# atlas-stage

Read the shared human-intent, persistence-approval, runtime, provenance, curation-safety and clear-writing contracts plus staging root policy and the selected bucket README/template/index. This is the write step behind **Teach Atlas**. Do not delegate.

1. For a direct request, show the shared concrete preview and obtain scope-bound approval. For an internal handoff, verify the passed approval matches exact claims/files/targets and do not ask again. A reusable Questions handoff also carries a complete duplicate-search ledger: staging statuses/buckets and curated types/indexes searched; ephemeral file-set-and-content freshness fingerprints for every searched surface; qualified question IDs; matching staging IDs/paths/statuses; curated candidate IDs/pages/ambiguity; selected target IDs/pages/baseline statuses; and unresolved candidates. Re-preview material scope changes.
2. Complete the shared taxonomy/contract and destination-contract reads, then the shared post-approval branch/HEAD/content-clean recheck. Immediately before ID allocation or any write, run the duplicate gate. A direct request, incomplete ledger or changed handoff repeats the full staging, typed curated `find`, exact-route and relevant-index duplicate search. For a complete Questions handoff, compare every ephemeral freshness fingerprint with the current searched surface: a missing or changed fingerprint requires full duplicate discovery; only when every fingerprint matches recheck just output-path uniqueness, exact qualified-question references, known duplicate states, and selected target ID/page/status against its baseline. Return a revised preview when rediscovery or a narrow recheck reveals a new match, target change, ambiguity, or a known duplicate-status or other change that materially changes approved claims, files, targets, evidence boundary, destructive effects or checkpoint range; otherwise carry the existing approval. A changed branch, HEAD or worktree after this gate repeats it before writing. Never persist the fingerprints, silently select ambiguity, or write before any required revised preview is approved. Choose the primary-fact bucket and split only independent boundaries. Route broad investigation to repository onboarding and incremental merged-default-branch ranges to `atlas-stage-changes`.
3. Allocate a deterministic staging ID and draft once with the clear-writing contract: capture one coherent curation-ready record with readable meaning, precise source references, causal boundaries, known/possible states, gaps and suggested targets. For question-derived evidence, cite every originating qualified `<record-id>#<question-id>` in the body.
4. Flag sensitive artifacts without values. Never revise committed evidence or write curated/generated files. Validate, then create the shared exact-path local commit on the selected feature branch.
5. Use the shared plain-language completion summary; expose the branch/commit, record ID, lifecycle and duplicate routes as audit detail.
