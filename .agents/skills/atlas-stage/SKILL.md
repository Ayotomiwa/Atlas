---
name: atlas-stage
description: Use only after an explicit request to capture one coherent reusable datalens engineering fact as attributable staging evidence without curating it.
---

# atlas-stage

Read the shared human-intent, persistence-approval, runtime and provenance contracts plus staging root policy and the selected bucket README/template. This is the write step behind **Teach Atlas**. Do not delegate.

1. For a direct request, show the shared concrete preview and obtain scope-bound approval. For an internal handoff, verify the passed approval matches exact claims/files/targets and do not ask again. A reusable Questions handoff also carries a complete duplicate-search ledger: staging statuses/buckets and curated types/indexes searched; qualified question IDs; matching staging IDs/paths/statuses; curated candidate IDs/pages/ambiguity; selected targets; and unresolved candidates. Re-preview material scope changes.
2. A direct request, incomplete ledger or changed handoff repeats the full staging, typed curated `find`, exact-route and relevant-index duplicate search. A complete unchanged Questions handoff rechecks only output-path uniqueness, exact qualified-question references, known duplicate states, and selected target ID/page/status. Any new match, target change, ambiguity or material scope change invalidates the narrow path: repeat full duplicate discovery and return a revised preview. Never silently select ambiguity. Choose the primary-fact bucket and split only independent boundaries. Route broad investigation to repository onboarding and incremental merged-default-branch ranges to `atlas-stage-changes`.
3. Allocate a deterministic staging ID and capture one coherent curation-ready record with readable meaning, precise source references, causal boundaries, known/possible states, gaps and suggested targets. For question-derived evidence, cite every originating qualified `<record-id>#<question-id>` in the body.
4. Flag sensitive artifacts without values. Never revise committed evidence or write curated/generated files.
5. Use the shared plain-language completion summary; expose the record ID, lifecycle and duplicate routes as audit detail.
